spring:
  datasource:
    url: jdbc:postgresql://10.226.80.34:5432/muhs
    username: muhs
    password: muhs
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update
